/**
 * Modelo del objeto Customer que contiene los datos del usuario.
 */
export class Customer {
    public name: string;
    public phoneNumber: string;
    public email: string;
    public customerId: string;
    public customerToken: string;
}